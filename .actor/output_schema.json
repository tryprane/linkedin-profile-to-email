{
  "actorOutputSchemaVersion": 1,
  "title": "LinkedIn Profile to Verified Email Output",
  "properties": {
    "results": {
      "type": "string",
      "title": "Extracted Lead Results",
      "description": "Dataset containing verified email addresses and contact information.",
      "template": "{{links.apiDefaultDatasetUrl}}/items?clean=true&format=json"
    },
    "overview": {
      "type": "string",
      "title": "Dataset Overview",
      "description": "Direct link to the default dataset for this run.",
      "template": "{{links.apiDefaultDatasetUrl}}"
    }
  }
}

{
  "title": "Mailmeteor LinkedIn Email Finder Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "linkedin_url": {
      "title": "LinkedIn Profile URL",
      "type": "string",
      "description": "The target LinkedIn profile URL (e.g. https://www.linkedin.com/in/satyanadella)",
      "editor": "textfield",
      "prefill": "https://www.linkedin.com/in/satyanadella"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "editor": "proxy",
      "description": "Select Apify residential proxy for Mailmeteor API querying.",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      }
    },
    "proxyChallenge": {
      "title": "Proxy the Turnstile challenge",
      "type": "boolean",
      "description": "Enable only if Mailmeteor rejects tokens minted from a different IP. This sends the much larger browser challenge through the proxy.",
      "default": false
    }
  },
  "required": ["linkedin_url"]
}
